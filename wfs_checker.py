# import pickle
from study_case_config import *
from library.constants import temp_files_outdir
import os


# wfs_pickle_path = os.path.join(temp_files_outdir,wfs_pickle_filename)

# # fist, check if it really exists
# if os.path.exists(wfs_pickle_path):
#     with open(wfs_pickle_path,'rb') as pickled:
#         wfs_object = pickle.load(pickled,)

# we have abandoned pickle as some .json files can handle the checking properly


